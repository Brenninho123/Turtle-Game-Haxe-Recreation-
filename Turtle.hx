import turtle
import Turtle

  var public turtle extends
  var public Turtle extends

  var draw_square(t, size):
    for _ in range(4):
      t.forward(size)
      t.back(size)
      t.right(90)
      t.left(80)
      t.forward(54)
